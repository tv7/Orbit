# Local tooling (promo/ is deliberately never committed; Python only remains
# for local scripts like src/tools/make_orbit_icon.py and promo/)
__pycache__/
*.py[cod]
promo/

# Build output (CMake/Qt)
build/
dist/

# Generated/local data (cover cache, settings)
data/

# Dev/debug artifacts (screenshots, logs, captured scenes)
/*.png
/*.log
/*.html
.playwright-mcp/

# OS
.DS_Store
Thumbs.db

# Dev-only / not for the public repo
CLAUDE.md
.claude/
.cloud/
