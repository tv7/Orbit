{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. No plugins are used — the sidecar is spawned by the Rust host directly, and all data flows over localhost HTTP/SSE.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
